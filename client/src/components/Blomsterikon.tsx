import styled from "styled-components";
import { FlowerPetalFallingIcon } from "@navikt/aksel-icons";

export const Blomsterikon = styled(FlowerPetalFallingIcon)`
  /* Vertikalt sentrert plassering relativ til tekst */
  position: relative;
  top: 0.17em;
  left: 0.1em;
`
